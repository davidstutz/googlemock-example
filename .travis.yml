language: cpp

compiler:  
    - gcc

before_script:
    - git submodule update --init --recursive
    - sudo apt-get install build-essential
    - sudo apt-get install cmake
    - mkdir build
    - cd build
    - cmake ..
    - make

script: ./tests/order 
